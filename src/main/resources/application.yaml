camunda:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/camunda
    username: postgres
    password: postgres
    configuration.maximum-pool-size: 30
domain:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/domaindata
    username: postgres
    password: postgres
    configuration.maximum-pool-size: 30   
spring:
  jpa:
    hibernate:
      ddl-auto: create
      hbm2ddl.auto: create   
    show-sql: true
    generate-ddl: true

camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
    lastName: Demo
  filter:
    create: All Tasks
server.port: 8080

